#pragma once

namespace adapters {

// Shared enum for Sketch3D view plane selection (used by UI and renderer).
enum class Sketch3DPlane
{
    XY = 0,
    YZ = 1,
    XZ = 2
};

} // namespace adapters
